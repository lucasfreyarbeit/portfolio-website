<a
    href="#barrierefreiheit"
    class="accessibility"
>
    DARSTELLUNG
</a>

<style>
.accessibility {
    position: relative;
    display: inline-block;

    font-family: "Roboto Mono", monospace;
    font-size: 1rem;
    font-weight: 400;

    color: inherit;
    text-decoration: none;

    cursor: pointer;

    transition: font-weight .25s ease;
}

.accessibility::after {
    content: "";

    position: absolute;
    left: 0;
    bottom: -4px;

    width: 100%;
    height: 2px;

    background: currentColor;

    transform: scaleX(0);
    transform-origin: right;

    opacity: 0;

    transition:
        transform .35s cubic-bezier(.25,1,.2,1),
        opacity .15s ease;
}

.accessibility:hover {
    font-weight: 500;
}

.accessibility:hover::after {
    transform: scaleX(1);
    transform-origin: left;
    opacity: 1;
}

.accessibility:focus-visible {
    outline: 2px solid currentColor;
    outline-offset: 4px;
}
</style>